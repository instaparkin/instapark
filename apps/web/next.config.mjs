/** @type {import('next').NextConfig} */
const nextConfig = {
    images : {
        remotePatterns : [
            {
                protocol : "https",
                hostname : "g-33ra03svqjy.vusercontent.net",
            }
        ]
    }
};

export default nextConfig;
