"use client";

import { AddSpace, CreatePayment, Skeleton } from "@instapark/ui";
import dynamic from "next/dynamic";

export default function Home(): JSX.Element {
  return (
    <main>
    </main>
  );
}
