import { axios } from "@instapark/utils"
import { beforeAll, describe, expect, test } from "vitest"

const AUTH_SERVER_URL = "http://localhost:8081/auth"

describe("Metadata Route", async () => {
    
});