{
  "name": "@instapark/map-server",
  "version": "1.0.0",
  "scripts": {
    "dev": "tsx watch src/map-server.ts",
    "build": "rm -rf dist && tsc",
    "lint": "eslint .",
    "check-types": "tsc --noEmit --pretty"
  },
  "dependencies": {
    "@instapark/auth": "workspace:^",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "tsx": "^4.19.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.0"
  }
}
